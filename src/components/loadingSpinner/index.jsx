import { StyledContainer, LoadingSpinner } from "./styles";

const Loading = () => {
   return (
      <StyledContainer>
         <LoadingSpinner />
      </StyledContainer>
   );
};

export default Loading; 